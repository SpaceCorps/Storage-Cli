[package]
name = "storage"
version = "1.0.0"
edition = "2024"
rust-version = "1.89"
description = "CLI for Azure Blob Storage - upload files and folders with time-limited read-only SAS URLs"
license = "MIT"
repository = "https://github.com/SpaceCorps/Storage-Cli"
readme = "README.md"

[[bin]]
name = "storage"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6", default-features = false, features = ["std", "derive", "help", "usage", "error-context", "suggestions", "color", "wrap_help"] }
indexmap = { version = "2", features = ["serde"] }
rpassword = "7.5"
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
serde_norway = "0.9"
ureq = { version = "3.4", default-features = false, features = ["rustls", "gzip"] }
zip = { version = "2.2", default-features = false, features = ["deflate"] }

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_Foundation", "Win32_Security_Cryptography"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
